/**
 * One expanded measure for the shell and public-share rail. Children fill their
 * container so the desktop width never leaks into the mobile sheet. Rem units
 * preserve the user's font scaling without shrinking text or pointer targets.
 */
export const RAIL_WIDTH = "w-[23.8rem]";
